@model IEnumerable<Power.Models.Tags>
@{
    ViewBag.Title = "标签列表";
    Layout = "~/Pages/_Layout.cshtml";
}

<script type="text/javascript">
    function deleteFun(id) {
        if (confirm("确定删除吗？")) { 


        }
    }
</script>
<p>
    <a href="/Tags/Create">Create New</a>
</p>
@Html.AntiForgeryToken()
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Remark)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.BlogNum)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Creator)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.U_CreateDate)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Remark)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BlogNum)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Creator)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.U_CreateDate)
            </td>
            <td>
                @Html.ActionLink("编辑", "Edit", new {  id=item.Id  }) | <a href="javascript:deleteFun('@item.Id')" >删除</a>
                
            </td>
        </tr>
}
    </tbody>
</table>
